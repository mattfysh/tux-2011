<form>
	<span class="name"><input name="name" value="{{ name }}" /></span>
	<span class="balance"><input name="balance" value="{{ tux.util.formatCurrency(balance) }}" /></span>
	<input type="submit" />
</form>